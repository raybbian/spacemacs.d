;;; packages.el --- competitive layer packages file for Spacemacs.  -*- lexical-binding: t -*-

;; This layer adds no packages; it builds a compile/run loop on top of
;; `compilation-mode' and `make-process', both of which are built in.

(defconst competitive-packages '())

;;; packages.el ends here
